nvui:
+ get chadrc.lua to work
* treesitter nvui cfg
- lspconfig nvui cfg
+ fix squigglys
+ fix tiny-inline-diagnostics
+ fix minty or go back to ccc
+ set up NvDash
+ customize stl
* test lsp signature
- fix lsp renamer
- add cmp styles
- themepicker bind (also fix error on confirm)
- theme toggler bind
~ cheatsheet [MEH... disable/ignore?]
- gitsigns nvui cfg
- telescope nvui cfg
+ disable/customize bufline

Kanagawa:
- fix gutter icon bg
- fix ibl thickness
- fix TMP_Text type wrong color
- fix C# parens/braces color
- fix c ptr variable name color
- change which-key color scheme, red font is distracting

Statusline:
- mode
- filename
- number of diagnostics
- connected LSP
- cwd dir
- git branch, additions

- Dashboard

C#:
- fix diagnostics only updating after changing buf contents (see roslyn.nvim repo)
- test roslyn formatter reliability
- code lens...
- unity integration.......
- debugging...

- find references over telescope

tiny-code-action:
- add delta for code action diff syntax highlighting

tiny-glimmer:
- look into rare undo bug

- look into CHADTTree

- exit visual mode on capslock
- cancel search on capslock

- try out harpoon

- write shada.X cleanup tool

- nvim-cmp cmdline:

{
    "hrsh7th/nvim-cmp",
    dependencies = {
      -- this
      {
        "hrsh7th/cmp-cmdline",
        event = "CmdlineEnter",
        config = function()
          local cmp = require "cmp"

          cmp.setup.cmdline("/", {
            mapping = cmp.mapping.preset.cmdline(),
            sources = { { name = "buffer" } },
          })

          cmp.setup.cmdline(":", {
            mapping = cmp.mapping.preset.cmdline(),
            sources = cmp.config.sources({ { name = "path" } }, { { name = "cmdline" } }),
            matching = { disallow_symbol_nonprefix_matching = false },
          })
        end,
      },
    },
  },
